spring:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    # MySQL 配置
    url: jdbc:mysql://134.175.125.238:3306/zhaoxin?characterEncoding=utf8&useSSL=false&serverTimezone=UTC&allowMultiQueries=true
    username: root
    password: '@qg2018QG'
    driver-class-name: com.mysql.jdbc.Driver
    initialSize: 5
    minIdle: 5
    maxActive: 100
    maxWait: 60000
    timeBetweenEvictionRunsMillis: 60000
    minEvictableIdleTimeMillis: 300000
    validationQuery: SELECT 1 FROM DUAL
    testWhileIdle: true
    testOnBorrow: false
    testOnReturn: false
    poolPreparedStatements: true
    maxPoolPreparedStatementPerConnectionSize: 20
    filters: stat,wall,log4j
    connectionProperties: druid.stat.mergeSql=true;druid.stat.slowSqlMillis=5000
  # Redis 配置
  redis:
    database: 0
    host: 134.175.125.238
    port: 10086
    password: root
    jedis:
     pool:
      max-active: 300
      max-wait: -1
      max-idle: 8
      min-idle: 0
    timeout: 5000